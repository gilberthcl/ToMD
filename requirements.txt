# Core conversion dependencies
extract-msg>=0.48      # .msg  (Outlook emails)
pymupdf>=1.24          # .pdf
python-docx>=1.1       # .docx (Word)
striprtf>=0.0.26       # .rtf
markdownify>=0.11      # .html/.htm

# Plain text (.txt/.log) needs nothing extra.
#
# Optional: install the `pandoc` binary to also convert .odt, .doc, .epub,
# .tex, and more:  brew install pandoc
